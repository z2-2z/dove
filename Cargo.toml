[package]
name = "dove"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
debug = true

[dependencies]
clap = { version = "4.4.1", features = ["derive", "wrap_help", "env"] }
